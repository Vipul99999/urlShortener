{
  "name": "@repo/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "build": "pnpm --filter @repo/db generate && tsc -p tsconfig.json",
    "start": "node dist/worker.js"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/shared": "workspace:*",
    "@sentry/node": "^10.49.0",
    "dotenv": "^16.4.5",
    "nodemailer": "^8.0.5"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "@types/nodemailer": "^8.0.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
